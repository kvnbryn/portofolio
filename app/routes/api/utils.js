// Fungsi untuk mengambil data (dalam hal ini dari Pixiv, atau bisa disesuaikan)
const getPixivData = async () => {
  // Kode untuk mengambil data dari API eksternal atau database
  return {
    id: 1,
    name: 'Koharu',
    description: 'Sample data for Koharu model'
  };
};

module.exports = { getPixivData };
